{
  "name": "maxpy-share",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Cloudflare Worker: short links for MaxPy patches.",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260924.1",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.38.0"
  }
}
